<Window x:Class="PinMap.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PinMap"
        xmlns:contr="clr-namespace:PinMap.UserControls"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 툴바 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5">
            <Button Content="데이터 로드"
                    Command="{Binding LoadDataCommand}"
                    Margin="5,0" Padding="10,3"/>
            <Button Content="뷰 초기화"
                    Command="{Binding ResetViewCommand}"
                    Margin="5,0" Padding="10,3"/>
            <Button Content="데이터 삭제"
                    Command="{Binding ClearDataCommand}"
                    Margin="5,0" Padding="10,3"/>
        </StackPanel>

        <!-- 맵 컨트롤 -->
        <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1" Margin="5">
            <contr:PointDisplayControl
                x:Name="MapControl"
                Points="{Binding MyPoints}"
                ChannelColors="{Binding MyColors}"/>
        </Border>

        <!-- 상태바 -->
        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusText}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
